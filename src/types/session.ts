export interface Session {
    id: string
    appointment_id: string
    subjective: string
    objective: string
    assessment: string
    plan: string
    pain_level: number
    created_at: string
}
